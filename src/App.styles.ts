import styled from 'styled-components';

export const AppContainer = styled.div`
    max-width: 100vw;
    overflow-x: hidden;
    padding: 0 1rem;
    box-sizing: border-box;

    h1 {
        text-align: center;
        width: 100%;
    }
`; 